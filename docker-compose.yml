version: '3.8'

services:
  subscriptions:
    build: .
    image: subscriptions-app
    container_name: subscriptions
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    environment:
      - PORT=5000
