version: '3.8'

services:
  remainders:
    build: .
    image: remainders-app
    container_name: remainders
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    environment:
      - PORT=5000
