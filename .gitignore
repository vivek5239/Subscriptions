# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
build
dist
out

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
docker-compose.override.yml

# IDEs
.idea
.vscode
*.swp
*.swo

# Data (Don't commit the actual data if it's sensitive, but user asked to use subscriptions.json)
# We will commit subscriptions.json as seed data, but usually data/* is ignored.
# keeping data/subscriptions.json tracked for now as per context.
